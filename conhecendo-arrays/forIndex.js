function imprimeIndexEElemento(lista){
    for (let i = 0; i < lista.length; i++) {
        console.log(`índice ${i} | elemento ${lista[i]}`);
      
    }
}

const meuArray = ['paulo','sergio','valdomiro'];

imprimeIndexEElemento(meuArray);